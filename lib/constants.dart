import 'package:flutter/material.dart';

const kTextFieldPadding = EdgeInsets.only(left: 20, right: 20);

const kTextFieldContPadding = EdgeInsets.only(left: 20);

final kCustomButtonColor = MaterialStateProperty.all(Colors.black);
